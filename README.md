# 安装

```shell
pip install graphrag


graphrag init --root .


graphrag index --root .

graphrag query --root . --method local --query "graphrag 测绘合同相关条款"
--verbose
--dry-run / --no-dry-run    
 --show-completion 
```
