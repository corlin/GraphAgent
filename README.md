# 安装

```shell
pip install graphrag


graphrag init --root .


graphrag index --root .

graphrag query --root . --method local --query "故事的主旨是什么？"
--verbose
--dry-run / --no-dry-run    
 --show-completion 
```
